Corrija a linha 262

Troque:

for (String[] g : ep.fieldGets) {

por:

for (FeatureParser.FieldGet g : ep.fieldGets) {

Dentro desse bloco, substitua:

g[0]

por:

g.nameVar

E substitua:

g[1]

por:

g.expr

Exemplo:

for (FeatureParser.FieldGet g : ep.fieldGets) {
    Map<String, Object> fieldGet = new LinkedHashMap<>();

    fieldGet.put("nameVar", g.nameVar != null ? g.nameVar : "");
    fieldGet.put("expr", g.expr != null ? g.expr : "");

    fieldGets.add(fieldGet);
}

Use o nome da sua lista atual no lugar de fieldGets, caso seja diferente.

Corrija a linha 273

Troque:

for (String[] s : ep.fieldSets) {

por:

for (FeatureParser.FieldSet s : ep.fieldSets) {

Dentro desse bloco, faça as substituições:

s[0] → s.pathExpr
s[1] → s.value
s[2] → s.before
s[3] → s.beforeEndpoint

Exemplo:

for (FeatureParser.FieldSet s : ep.fieldSets) {
    Map<String, Object> fieldSet = new LinkedHashMap<>();

    fieldSet.put("pathExpr", s.pathExpr != null ? s.pathExpr : "");
    fieldSet.put("value", s.value != null ? s.value : "");
    fieldSet.put("before", s.before != null ? s.before : "");
    fieldSet.put(
        "beforeEndpoint",
        s.beforeEndpoint != null ? s.beforeEndpoint : ""
    );

    fieldSets.add(fieldSet);
}
Resumo direto

No FlowEditorBridge.java:

for (String[] g : ep.fieldGets)

vira:

for (FeatureParser.FieldGet g : ep.fieldGets)

E:

for (String[] s : ep.fieldSets)

vira:

for (FeatureParser.FieldSet s : ep.fieldSets)
